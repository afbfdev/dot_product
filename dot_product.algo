ALGORITHM dot_product
VAR
    v1 := Array INTEGER [];
    v2 := Array INTEGER [];
    ps : INTEGER := 0;
BEGIN
    FOR i FROM 0 TO v1.lenght-1 STEP 1  DO
    FOR i FROM 0 TO v2.lenght-1 STEP 1  DO
    ps := ps + v1[i]*v2[i]  
    END_FOR
        
    END_FOR
    RETURN ps
END